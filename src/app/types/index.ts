/**
 * TypeScript Type Definitions
 * Framework-specific types (React Native, Expo, third-party libraries)
 * For application models/entities, see ../models
 */

// Currently no framework-specific types
// Add declarations here for:
// - React Navigation types
// - Expo-specific augmentations
// - Third-party library type extensions

