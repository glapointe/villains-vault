namespace Falchion.Villains.Vault.Api.Enums;

/// <summary>
/// Sort direction for queries.
/// </summary>
public enum SortDirection
{
	/// <summary>Sort in ascending order (A-Z, 0-9, earliest to latest)</summary>
	Ascending,
	
	/// <summary>Sort in descending order (Z-A, 9-0, latest to earliest)</summary>
	Descending
}
